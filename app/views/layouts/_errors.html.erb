<br>
<% if obj.errors.any? %>
  <h3>The following <%= obj.errors.size > 1 ? 'errors' : 'error' %> occurred:</h3>
  <ul>
    <% obj.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <br><br>
<% end %>
