<h1><%= klass_heading_and_link(@klass) %></h1>
<h2>Student List</h2>
<%= link_to_add_student_status(@klass) %>
<% @student_statuses.each do |ss| %>
  <h3><%= link_to ss.last_name_first, klass_student_status_path(@klass, ss) %></h3>
<% end %>
